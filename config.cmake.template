# Used for FTP
set(SWITCH_IP 192.168.1.1)

# Used for logging
set(LOGGER_IP 192.168.1.1)
set(LOGGER_PORT 5123)

set(SWITCH_TARGET fee_120)

add_compile_definitions(
        # AFFECTION_ENABLED=true #for toggles
        # EXP_SHARE_ENABLED=true 
)
